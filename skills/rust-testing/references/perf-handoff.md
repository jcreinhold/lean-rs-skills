# Performance Handoff

Use `rust-performance` when the missing guardrail measures time, allocation, memory, or scaling. Keep a correctness regression here when the performance bug also changed behavior.
