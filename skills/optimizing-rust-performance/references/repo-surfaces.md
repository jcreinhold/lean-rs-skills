# Repository Measurement Surfaces

Search for Criterion benches, profiling crates, existing timing hooks, and tests close to the suspected path. Reuse their fixtures and commands when they cover the claim. Add a focused bench or hook when no existing surface is both repeatable and representative. Keep a pipeline workload for confirmation when a focused bench cannot reveal shifted costs.
